# Código Legado (ChatPy Pré-Reescrita)

Este diretório contém o código, configurações e dados da versão original do ChatPy (pré-reescrita V2).

> [!WARNING]
> Este código é mantido estritamente para referência histórica. Ele não faz parte da arquitetura atual baseada em FastAPI/WebSockets e não deve ser utilizado como base para novas implementações ou evoluções do sistema.
